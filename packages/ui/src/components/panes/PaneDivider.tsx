import { Component } from "solid-js";

/** Draggable split handle — Phase 2 implementation */
const PaneDivider: Component = () => {
  return <div class="pane-divider" />;
};

export default PaneDivider;
